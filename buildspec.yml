version: 0.2
phases:
    build:
        commands:
          - echo Hello World!
          - aws s3 cp s3://rigd-tools-east/HWTemplate.yaml template.yaml
artifacts:
    files:
      - '__init__.py'
      - 'HelloWorld.py'
